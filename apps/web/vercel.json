{
  "buildCommand": "cd ../.. && npx turbo build --filter=@cardeko/web --force",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://cardeko.vercel.app/api/:path*" },
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
